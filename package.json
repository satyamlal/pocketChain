{
  "name": "pocket-chain-workspace",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*/pkg"
  ],
  "scripts": {
    "build:core" : "cd packages/core && wasm-pack build --target web",
    "dev:frontend" : "cd apps/frontend && bun run dev"
  }
}